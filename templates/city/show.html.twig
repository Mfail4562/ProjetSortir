{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
    {#    <h1>City</h1> #}

    {#    <table class="table"> #}
    {#        <tbody> #}
    {#            <tr> #}
    {#                <th>Id</th> #}
    {#                <td>{{ city.id }}</td> #}
    {#            </tr> #}
    {#            <tr> #}
    {#                <th>Name</th> #}
    {#                <td>{{ city.name }}</td> #}
    {#            </tr> #}
    {#            <tr> #}
    {#                <th>ZipCode</th> #}
    {#                <td>{{ city.zipCode }}</td> #}
    {#            </tr> #}
    {#        </tbody> #}
    {#    </table> #}

    {#    <a href="{{ path('app_city_index') }}">back to list</a> #}

    {#    <a href="{{ path('app_city_edit', {'id': city.id}) }}">edit</a> #}

    {#    {{ include('city/_delete_form.html.twig') }} #}
    <section class="border-white">
        <br>
        <h1 class="text-center ">{{ city.name }}</h1>
        <div class="container py-5 border-white">
            <div class="row ">
                <div class="col-lg-4 ">
                    <div class="card mb-4 border-white">
                        <div class="card-body text-center ">
                            <img
                                    src="{{ asset('img/travelLogo/city.jpg') }}"
                                    alt="avatar" class="rounded-circle img-fluid"
                                    style="width: 150px"/>
                            <h5 class="my-3">{{ city.name }}</h5>
                            <h5 class="my-3">{{ city.zipCode }}</h5>

                            <a class="btn btn-secondary m-2 my-sm-0 text-muted mb-1"
                               href="{{ path('app_city_index') }}">back to City list</a>
                            <a class="btn btn-secondary m-2 my-sm-0 text-muted mb-1"
                               href="{{ path('app_campus_edit', {'id': city.id}) }}">edit City</a>


                            {{ include('city/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
