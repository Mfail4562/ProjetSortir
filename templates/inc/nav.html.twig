<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand me-2" href={{ path('app_main_index') }}>
            <img
                    src={{ asset('img/avion.png') }}
                    height="16"
                    alt="MDB Logo"
                    loading="lazy"
                    style="margin-top: -1px;"
            />
        </a>
        <a class="navbar-brand" href="{{ path('app_main_index') }}">Travel.com</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03"
                aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_main_index') }}" title="Home">Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_place_index') }}" title="place">Place</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_campus_index') }}" title="campus">Campus</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_travel_index') }}" title="travel">Travel</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.firstname }}
                        )</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_show',{id:app.user.id}) }}" title="Profile">Profile

                        ({{ app.user.pseudo }})</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_dashboard') }}" title="Admin Dashboard">Admin</a>
                    </li>
                {% endif %}
            </ul>
            {% else %}
                <form class="d-flex align-items-center">

                    <a class="btn btn-secondary m-2 my-sm-0"
                       href="{{ path('app_login') }}" title="Login" role="button">Login</a>
                    <a class="btn btn-secondary m-2 my-sm-0"
                       href="{{ path('app_register') }}" title="Create account" role="button">Register</a>
                </form>
            {% endif %}
        </div>
    </div>
</nav>